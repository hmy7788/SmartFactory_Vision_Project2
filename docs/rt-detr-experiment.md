# RT-DETR 실험 결과

**브랜치**: `experiment/rt-detr`
**목적**: CLAUDE.md 3-1 "RT-DETR도 비교 후보로 고려 가능"에 따른 검증 — YOLO-OBB 대비 비교용 실험.

**결론**: 50 epoch로 예약했었지만, epoch 10에서 의도적으로 중단하고(`--epochs 50`이었으나 조기 종료) 그 체크포인트로 테스트까지 마쳤다. 아래는 그 최종 기록.

---

## 실험 설정

| 항목 | 값 |
|---|---|
| 모델 | `rtdetr-l.pt` (ultralytics) |
| 데이터 | ICONIC 500장, 5클래스 (볼트_주황/볼트_노랑/나무_5구멍/나무_3구멍/나무_2구멍) |
| 라벨 형식 | **AABB** — 원본은 OBB인데 RT-DETR이 OBB를 지원하지 않아 4꼭짓점 min/max로 변환 (`prepare_rtdetr_dataset.py`) |
| Train / Val | 425장 / 75장 (val 15%, seed 42) — CLAUDE.md의 "Train/Test" 방침에서 Test에 해당 |
| 하이퍼파라미터 | epochs=50(예약), batch=8, imgsz=640 → **실제로는 epoch 10에서 중단** |
| 하드웨어 | RTX 4050 Laptop GPU (6GB VRAM) |
| 학습 스크립트 | `src/detection/rt-detr/train_rtdetr.py` |

## 알려진 제약

- AABB로 변환했기 때문에, 회전된 부품(특히 45°/135° 각도로 놓인 나무 막대)은 박스가 실제보다 헐겁게 잡힘 — YOLO-OBB 대비 불리한 조건에서 비교하는 실험.
- val 75장은 소량이라 mAP 수치가 epoch마다 변동이 큼(노이즈 있음).

## Epoch별 결과 (1~10, 학습 종료 시점)

`runs/rtdetr/main_run/results.csv` 기준. 10 epoch 총 소요시간 약 1980초(**33분**).

| epoch | precision | recall | mAP50 | mAP50-95 | 누적 소요(초) |
|---|---|---|---|---|---|
| 1 | 0.2335 | 0.6533 | 0.3615 | 0.3241 | 116 |
| 2 | 0.2766 | 0.7721 | 0.4145 | 0.3387 | 294 |
| 3 | 0.5481 | 0.9554 | 0.8071 | 0.7196 | 477 |
| 4 | 0.5460 | 0.6814 | 0.5711 | 0.4560 | 685 |
| 5 | 0.5837 | 0.6400 | 0.6106 | 0.5043 | 905 |
| 6 | 0.6653 | 0.7951 | 0.7625 | 0.6697 | 1123 |
| 7 | 0.8869 | 0.9784 | 0.9819 | 0.8936 | 1375 |
| 8 | 0.9026 | 1.0000 | 0.9950 | 0.9207 | 1583 |
| 9 | 0.8216 | 0.8768 | 0.9223 | 0.8507 | 1775 |
| **10** | **0.9622** | **1.0000** | **0.9925** | **0.9286** | 1980 |

- mAP50이 epoch 4~5에서 한 번 떨어졌다가(0.57→0.61) epoch 7부터 다시 급격히 올라 0.98 이상으로 안정화됨. val 75장의 작은 표본 때문에 변동성이 있는 것으로 보임.
- epoch당 평균 약 207초(첫 epoch은 모델 로드 등 오버헤드 포함이라 더 짧게 잡힘).

## 실제 추론 테스트 (epoch 10 체크포인트, `last.pt`)

val 지표(precision/recall/mAP)는 높게 나왔지만, 클래스당 1장씩 5장을 직접 추론해서 눈으로 확인했다.

| 샘플 | 결과 |
|---|---|
| 나무_2구멍 | ✅ 정답 감지(conf 0.81), **단 나무_3구멍으로도 동시에 오탐지(conf 0.59)** — 같은 박스 좌표에 클래스 혼동 |
| 나무_3구멍 | ✅ 정답 감지(conf 0.86), **단 나무_2구멍으로도 동시에 오탐지(conf 0.48)** — 같은 박스 좌표에 클래스 혼동 |
| 나무_5구멍 | ✅ 단일 정답만 감지(conf 0.90) |
| 볼트_노랑 | ✅ 단일 정답만 감지(conf 0.90) |
| 볼트_주황 | ✅ 단일 정답만 감지(conf 0.86) |

**발견한 문제**: `나무_2구멍`과 `나무_3구멍` 이미지에서, 모델이 **완전히 동일한 bbox 좌표**에 대해 두 클래스를 동시에 confidence 0.25(threshold) 이상으로 예측함 (좌표 확인: 두 예측의 `xyxy`가 정확히 일치). 같은 재질·색상에 구멍 개수만 다른 두 부품을 아직 명확히 구분 못 하는 것으로 보인다. 볼트류와 5구멍 막대는 이 문제가 없었다.

**해석**: val 지표(mAP50 0.99)만 보면 문제없어 보이지만, 실제 추론에서는 클래스 혼동이 남아있었다 — val 셋이 75장으로 작아서 이런 미세한 문제를 못 잡아낸 것으로 보인다. epoch을 늘리면(원래 예약했던 50까지) 개선될 가능성이 높으나, 이번 실험은 epoch 10에서 의도적으로 멈췄으므로 확인은 못 함.

결과 이미지: `runs/rtdetr/epoch10_sample/`

## 결과 파일 위치

- 가중치: `runs/rtdetr/main_run/weights/{best,last}.pt`
- 전체 지표: `runs/rtdetr/main_run/results.csv`
- 학습 설정: `runs/rtdetr/main_run/args.yaml`
- 배치 샘플 미리보기: `runs/rtdetr/main_run/train_batch{0,1,2}.jpg`

## TODO (1차 실험)

- [x] epoch 10 기준 최종 precision/recall/mAP50/mAP50-95 기록 (50까지는 안 돌림)
- [x] epoch 10 체크포인트로 실제 추론 테스트 (클래스별 샘플 1장씩)
- [x] 나무_2구멍/나무_3구멍 클래스 혼동 문제 — **2차 실험(아래)에서 해결됨**
- [ ] YOLO-OBB 결과와 나란히 비교표 작성 (정확도, 추론 속도, 학습 시간)
- [x] `runs/`, `datasets/`가 `.gitignore`에 걸려있는지 확인 — 대용량 가중치/복사된 이미지가 커밋되지 않도록 (`datasets/`는 이번에 추가함)

---

# 2차 실험: 통합 데이터셋 (iconic + assembled_labeled)

**목적**: 1차 실험에서 발견된 나무_2구멍/나무_3구멍 클래스 혼동 문제를, 조립체(여러 부품이 한 프레임에 섞인) 사진을 추가 학습시키면 해결되는지 확인.

## 데이터

| 소스 | 내용 | 장수 |
|---|---|---|
| `data/iconic/` | 부품 1개짜리 ICONIC 사진, OBB→AABB 변환 | 559장 (bolt_1 130, bolt_2 129, mother_part 100, part_2hole 100, part_3hole 100) |
| `data/assembled_labeled/` | 조립체 사진, 이미 AABB, 사람이 직접 라벨링 | 100장 (Model A 20, Model B 40, Model C 40) |
| **합계** | | **659장** |

Train/Val(=Test) 분할은 **소스별·클래스(모델)별로 각각 15%씩** — 전체를 한 덩어리로 섞어서 나누면 소수 그룹(Model A 20장 등) 비율이 틀어질 수 있어서, iconic은 클래스별로 / assembled_labeled는 `model_a/b/c` 파일명 접두사 기준으로 각각 나눔. train 560장 / val 99장.

스크립트: `src/detection/rt-detr/prepare_rtdetr_dataset.py` (두 소스를 합치도록 확장), 출력: `datasets/rtdetr_combined/`

## 하이퍼파라미터

epochs=10, batch=8, imgsz=640, 모델 `rtdetr-l.pt`. 1차 실험과 같은 조건(epoch 10)으로 맞춰서 비교 가능하게 함.

## Epoch별 결과

`runs/rtdetr/combined_run/results.csv` 기준. 10 epoch 총 소요 2562초(**약 43분** — 1차 실험보다 데이터가 32% 많아서 더 오래 걸림).

| epoch | precision | recall | mAP50 | mAP50-95 |
|---|---|---|---|---|
| 1 | 0.2181 | 0.5098 | 0.3720 | 0.3035 |
| 2 | 0.5973 | 0.7956 | 0.7468 | 0.6389 |
| 3 | 0.8960 | 0.9314 | 0.9257 | 0.7791 |
| 4 | 0.9466 | 0.9369 | 0.9592 | 0.7884 |
| 5 | 0.9347 | 0.9606 | 0.9547 | 0.8048 |
| 6 | 0.9726 | 0.9795 | 0.9788 | 0.8603 |
| 7 | 0.9785 | 0.9862 | 0.9809 | 0.8667 |
| 8 | 0.9828 | 0.9827 | 0.9814 | 0.8733 |
| 9 | 0.9873 | 0.9862 | 0.9817 | 0.8795 |
| **10** | **0.9764** | **0.9862** | **0.9800** | **0.8762** |

1차 실험 대비 mAP50-95가 소폭 높고(0.876 vs 0.929는 1차 epoch10 val 지표라 직접 비교는 표본이 달라 주의), 더 중요한 건 아래 실제 추론 테스트 결과.

## 실제 추론 테스트 — 클래스 혼동 문제 해결 확인

클래스별 단일 부품 5장 + 조립체(Model A/B/C) 3장, 총 8장을 직접 추론.

| 샘플 | 결과 |
|---|---|
| 나무_2구멍 | ✅ 단일 정답만 감지 (conf 0.97) — **1차 실험의 동시 오탐 문제 사라짐** |
| 나무_3구멍 | ✅ 단일 정답만 감지 (conf 0.98) |
| 나무_5구멍 | ✅ 단일 정답만 감지 (conf 0.96) |
| 볼트_노랑 | ✅ 단일 정답만 감지 (conf 0.91) |
| 볼트_주황 | ✅ 단일 정답만 감지 (conf 0.93) |
| **Model A** (5부품: mother_part+part_3hole+part_2hole+bolt_1+bolt_2) | ✅ **5개 전부 정확히 검출** |
| **Model B** (5부품: mother_part+part_2hole×2+bolt_1×2) | ✅ **개수(×2)까지 정확히 검출** |
| **Model C** (3부품: mother_part+part_3hole+bolt_2) | ✅ **3개 전부 정확히 검출** |

**결론**: 1차 실험에서 나무_2구멍/나무_3구멍 사진에 같은 bbox로 두 클래스가 동시에 오탐되던 문제가, `assembled_labeled`(여러 부품이 나란히 비교되는 조립체 사진)를 추가 학습시키자 사라졌다. 단일 ICONIC 사진만으로는 배우기 어려웠던 두 클래스 구분을, 조립체 사진(비슷한 부품이 서로 다른 위치에 동시에 등장)이 보완해준 것으로 보인다. 조립체 사진에서 부품 개수(Model B의 part_2hole×2, bolt_1×2)까지 정확히 잡아낸 것도 주목할 만함.

## 결과 파일 위치

- 가중치: `runs/rtdetr/combined_run/weights/{best,last}.pt`
- 전체 지표: `runs/rtdetr/combined_run/results.csv`
- 추론 테스트 이미지(샘플 8장): `runs/rtdetr_test/combined_10ep/`
- 데이터셋: `datasets/rtdetr_combined/` (`.gitignore`에 걸려 커밋 안 됨, 재현하려면 `prepare_rtdetr_dataset.py` 재실행)

## 전체 val셋(99장) 추론 테스트

위 8장 샘플 확인에 이어, val셋 **99장 전체**를 `last.pt`로 추론해서 확인했다 (`src/detection/rt-detr/test_full_val.py`).

| 구분 | 장수 | 결과 |
|---|---|---|
| 단일 부품 (bolt_1/2, mother_part, part_2hole/3hole) | 79장 | ✅ 전부 박스 1개씩만 정확히 검출 |
| model_a (기대 5개) | 3장 | ✅ 전부 5개 정확히 검출 |
| model_b (기대 5개) | 6장 | ⚠️ 5장은 정확, **`model_b_029`는 7개 검출**(아래 참고) |
| model_c (기대 3개) | 6장 | ✅ 전부 3개 정확히 검출 |
| **합계** | **99장** | 검출 박스 총 149개, 박스 0개(완전 미검출) 이미지는 없음 |

### 발견한 문제 — `model_b_029` 중복 검출

기대 5개(mother_part×1, part_2hole×2, bolt_1×2)인데 7개가 나왔다. 좌표를 직접 찍어보니:

- `part_2hole` conf 0.955 @ `[527.6, 98.3, 817.0, 265.3]`
- `part_2hole` conf 0.929 @ `[529.1, 98.5, 816.3, 264.9]` ← 위와 거의 동일 좌표 (중복)
- `mother_part` conf 0.910 @ `[527.8, 108.1, 799.4, 679.2]`
- `mother_part` conf 0.854 @ `[528.8, 106.7, 798.5, 680.1]` ← 역시 거의 동일 좌표 (중복)
- `bolt_1` × 2 (정상, 서로 다른 위치)

1차 실험 때의 "**다른** 클래스가 같은 박스에 동시 검출"과는 다른 종류다 — 이번엔 **같은** 클래스가 소수점 수준으로만 다른 좌표에 중복 검출됐다. RT-DETR은 query 기반이라 원래 NMS 없이도 중복을 억제하도록 학습되는데, epoch 10만 돌려서 이 중복 억제가 아직 덜 수렴한 것으로 보인다. 99장 중 이 1장에서만 발생 — 이례적이지 결정적 문제는 아님.

결과 이미지: `runs/rtdetr_test/full_val/` (99장 전체)

## TODO (2차 실험)

- [x] 통합 데이터셋으로 클래스 혼동 문제 해결 확인
- [x] val셋 99장 전체 추론 테스트 — `model_b_029` 1장에서 중복 검출(같은 클래스, 근접 좌표) 발견, epoch을 늘리면 개선될지는 미확인
- [ ] Model A/B/C 조립체 사진을 더 다양한 각도/개수로 추가하면 더 개선되는지 (현재 100장뿐)
- [ ] epoch을 더 늘려도(20~50) 지표가 계속 개선되는지 확인 (지금은 10에서 멈춤) — 특히 위 중복 검출 문제 해소 여부
- [ ] YOLO-OBB와 최종 비교표 작성 (1차 TODO에서 이월)
