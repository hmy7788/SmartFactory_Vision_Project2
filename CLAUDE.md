# CLAUDE.md
 
이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 프로젝트 컨텍스트입니다.
 
## ⚠️ 작업 원칙 (가장 중요)
 
**이 문서에 명시되지 않은 부분, 또는 애매하게 해석될 수 있는 요구사항이 있으면 임의로 판단해서 구현하지 말고, 먼저 사용자에게 질문할 것.** 특히 아래 항목들은 반드시 확인 후 진행:
 
- 이 문서의 "아직 미정" 섹션에 있는 항목
- 수치/threshold 값이 명시 안 된 경우 (예: 정확한 구멍 개수, 정확한 타이밍)
- 여러 구현 방식이 가능해 보이는데 이 문서가 하나로 못박지 않은 경우
- 새로운 기능/파일을 추가해야 할 것 같은데 이 문서에 언급이 없는 경우
추측으로 진행한 뒤 나중에 틀렸다는 걸 알게 되면 되돌리는 비용이 더 크다. 애매하면 멈추고 묻는다.
 
---
 
## 프로젝트 개요
 
**이름**: 비전 기반 작업자 오조립 방지 및 공정 검증 시스템
 
**한 줄 정의**: 생산지시 정보와 머신비전을 결합해, 작업자가 잘못된 부품을 선택하거나 잘못된 위치에 조립하는 순간 카메라와 AI로 감지해 즉시 알려주는 Vision 기반 조립 지원 시스템.
 
**Poka-Yoke란**: 인간의 실수(휴먼 에러)를 애초에 발생하지 못하게 하거나, 발생하더라도 즉시 알아챌 수 있게 설계하는 실수 방지 기법. 불량을 검사하는 게 아니라 불량 발생을 사전에 방지하는 데 초점을 둔다.
 
**핵심 문제**: 다품종 생산에서는 제품 Variant(Model A/B/C)에 따라 필요한 부품과 조립 순서가 달라진다. 이 과정에서 오피킹(잘못된 부품 선택), 잘못된 위치 조립 등이 발생할 수 있다.
 
**개발 배경**: 개발 기간이 짧게 제한되어 있음. Model A를 먼저 end-to-end로 완성하는 것을 최우선 목표로 삼고, Model B/C 확장은 시간이 남으면 추가한다. Model A/B/C 딥러닝 분류 트랙은 확정 항목이다.
 
---
 
## 부품(클래스) 종류
 
| 클래스명 | 색상 | 길이/설명 |
|---|---|---|
| 볼트_주황 | 주황 | 긺(장) |
| 볼트_노랑 | 노랑 | 짧음(단) |
| 너트_파랑 | 파랑 | — |
| 나무_5구멍 | 원목색 | 구멍 5개 뚫린 막대 |
| 나무_3구멍 | 원목색 | 구멍 3개 뚫린 막대 |
| 나무_2구멍 | 원목색 | 구멍 2개 뚫린 막대 |
 
※ 나무 부품은 정사각 블록을 이어붙인 것이 아니라, **긴 막대 하나에 구멍이 여러 개 뚫려있는 형태**임. 조립 시 두 나무 막대를 L자 형태로 배치하고, 모서리를 볼트+너트로 고정한다. 이 구조가 3-2 룰베이스 로직 설계에 직접 영향을 줌 (아래 참고).
 
**Model A/B/C 정확한 구멍 개수는 아직 실측 확정 전 — 실제 부품 기준으로 재확인 필요 (아래 "아직 미정" 참고).**
 
---
 
## 화면 구역(Zone) 정의
 
카메라 화면을 y좌표 기준으로 위/아래 두 구역으로 나눈다:
 
- **부품 섹션 (Pick Zone)**: 화면 위쪽, 부품을 집는 구역
- **조립대 섹션 (Assembly Zone)**: 화면 아래쪽, 조립을 진행하는 구역
구역 판별은 **모든 클래스에 공통으로 적용되는 단일 규칙**이다 — 감지된 바운딩 박스 중심의 y좌표가 경계선(y_threshold) 위/아래인지만 비교하면 되고, 부품 종류별로 별도 로직을 만들 필요는 없다.
 
---
 
## 손 추적 (제외됨)

- **MediaPipe Hands 기반 손 추적은 포기하기로 확정.** 손-부품 연결(Association), 손이 든 부품 기준의 오피킹/조립 판정 로직은 구현하지 않는다.
- 구현하지 않는 것: `mediapipe_hand.py`, `hand_part_matcher.py`, 손 랜드마크 기반 판정 일체.
- 오피킹/스텝 완료 판정은 손이 아니라 아래 "조립 섹션 실시간 검증"으로 대체한다.

---

## 조립 섹션 실시간 검증 (오조립 판정의 핵심)

**조립 섹션(Assembly Zone)에 놓인 부품 조합이 Recipe와 맞는지를 매 프레임 실시간으로 검사한다.** 부품을 누가 어떻게 옮겼는지는 보지 않는다.

- **입력**: YOLO가 검출한 부품 중 bbox 중심 y좌표가 y_threshold 아래(조립 섹션)인 것들의 `{클래스: 개수}`.
- **기대값(Expected)**: Recipe에서 **현재 스텝까지**의 `expected_class`를 모은 `{클래스: 개수}`. (예: Model A 스텝 2 진행 중이면 `{나무_5구멍:1, 나무_3구멍:1}`)
- **비교는 클래스별 개수까지** 한다. 같은 클래스가 하나 더 있어도 NG (예: Model C는 나무_2구멍 2개).
- **NG**: 조립 섹션에 기대값에 없는 부품이 있거나 개수가 초과된 경우, 즉 아직 차례가 아닌 부품이나 잘못된 부품이 들어온 경우. 예) 부품 1, 2, 3이 필요한데 1, 2, 4가 조립 섹션에 있으면 NG.
- **PASS / 스텝 전환**: 조립 섹션 구성이 현재 스텝의 기대값과 정확히 일치하면 스텝 완료 → 다음 스텝으로 전환. 아직 덜 놓인 상태(기대값의 부분집합)는 NG가 아니라 진행 중이다.
- **NG 해제**: 잘못된 부품을 치워 구성이 다시 기대값과 맞으면 NG가 **자동 해제**되고 진행이 이어진다. (경고만 표시, 수동 리셋 없음)
- **디바운스**: 오검출/이동 중 경계선 통과로 인한 깜빡임을 막기 위해, NG/PASS 판정은 일정 프레임 또는 시간 이상 연속 유지될 때만 확정한다. 값은 config로 분리 (초기값은 아래 "아직 미정" 참고).
- NG일 때는 무엇이 잘못됐는지(예: "나무_3구멍이 예상되지만 볼트_노랑이 감지됨") 텍스트로 UI에 표시한다.

---

## State Machine (전체 흐름)

```
[UI] 작업지시서(UI)가 Model을 지정
     ↓
[STEP 시작] Recipe(JSON)에서 현재 Step 로드 → Expected {클래스: 개수} 계산
     ↓
[MONITOR] 매 프레임 조립 섹션 구성을 Expected와 비교
   ├── Expected에 없는 부품/초과 개수 (디바운스 후) → NG 표시 (원인 텍스트)
   │        └── 구성이 다시 맞으면 자동 해제 → MONITOR 계속
   ├── 일부만 놓임 (부분집합) → 진행 중
   └── Expected와 정확히 일치 (디바운스 후) → STEP 완료
     ↓
[STEP 반복] 모든 Step 완료까지 반복
     ↓
[VERIFY_FINAL] 룰베이스로 완성된 형태가 Recipe와 일치하는가?
   ├── YES → 작업 완료
   └── NO  → 구체적으로 어디가 틀렸는지 텍스트로 표시 (아래 참고) → 재시도 대기
```

---

## 데이터 수집 방법
 
- **카메라**: Logitech C270 웹캠, TOP-DOWN 고정, 오토포커스 OFF, 위치·초점·노출·조명 고정
- **배경**: **검은색 배경으로 확정.** ICONIC 촬영뿐 아니라 픽킹 영상, 그리고 **실제 배포·시연 환경도 검은 배경으로 통일**하기로 함 — 학습 데이터와 배포 환경이 일치하므로 도메인 시프트 문제 없음 (의도된 설계).
### 방식 1 — ICONIC (정적 촬영)
 
검은 배경, TOP-DOWN, 오토포커스 OFF, 위치 9곳(좌/중/우 × 상/중/하) × 방향 4종(0°/45°/90°/135°) = 클래스당 36장 기본 세트 + 여러 개 동시 배치 사진 추가
 
### 방식 2 — 픽킹 영상 (동적 촬영, 손 포함)
 
```
① 검은 배경 위 부품 놓인 상태 (3초)
② 손이 프레임에 들어옴 (2초)
③ 부품 집어드는 순간 (5초) ← 핵심 프레임
④ 손이 부품 들고 ROI 안에서 잠깐 정지 (5초)
⑤ 내려놓는 순간 (5초)
```
 
**이 데이터의 목적**: 손 추적은 사용하지 않으므로 손 인식용이 아님. **YOLO가 손에 부분적으로 가려진 상태에서도 부품을 정확히 인식하도록 강건성을 확보**하기 위한 것. 라벨링 시 손은 라벨링 대상이 아니고, 부품 바운딩 박스만 그리면 됨.
 
목표 수량(클래스별): 주황 볼트 100장 / 노랑 볼트 100장 / 파랑 너트 80장 / 나무_5구멍 60장 / 나무_3구멍 60장 / 나무_2구멍 60장 (영상 5프레임마다 1장 추출 기준)
 
⚠️ **미확인**: 이 프레임 추출 수치가 실제 웹캠 fps 기준으로 맞는지 검증 필요 (데이터 수집 담당자 확인 필요 — "아직 미정" 참고)
 
### 조립체 완성 사진 (딥러닝 분류 트랙용, 확정)

3-2 딥러닝 분류(확정)를 위해 **완성된 Model A/B/C 조립체 사진**이 별도로 필요함 (개별 부품 사진과는 다른 데이터셋). 클래스당 30~50장 정도, 각도/위치/조명을 살짝 바꿔가며 촬영.
 
### Train/Test 분할
 
**Val 없이 Train/Test만 분할.** 데이터 양이 적어 Val까지 나누면 학습 데이터가 부족해질 수 있다고 판단. (단, YOLO 학습 파이프라인 자체가 내부적으로 Train 일부를 자동으로 val로 떼어 쓰는 경우가 있는데, 이건 별도 데이터셋을 만든다는 의미가 아니라 학습 옵션의 일부이므로 방침과 상충하지 않음.)
 
---
 
## 머신비전 구현 방법
 
### 3-1. 오피킹 검출
 
- **모델**: YOLO 계열 (ex. YOLOv8/YOLO11 등 — 구체적 버전은 실험하며 선정). RT-DETR, Faster R-CNN도 비교 후보로 고려 가능.
- Bin(부품 섹션) + Assembly Area(조립대 섹션) 화면에서 부품 위치·클래스 동시 검출
- 조립 섹션의 Detected vs Expected(Recipe) 비교 → 불일치 시 NG (위 "조립 섹션 실시간 검증" 참고)
- 출력: bounding box + class + confidence
### 3-2. 조립 상태 확인 (최종 형태 검증)
 
**룰베이스는 필수 구현 항목.** (선택 사항 아님 — 프로젝트 요구조건)
 
#### 룰베이스 (필수)
 
- `cv2.findContours`를 `RETR_CCOMP` 모드로 사용해 나무 막대의 외곽 윤곽선과 내부 구멍(hole)을 분리 검출
- 구멍은 원형에 가까우므로 `cv2.HoughCircles` 또는 컨투어 면적/원형도 필터링으로 개수 카운트
- **"블록 개수 세기"가 아니라 "막대별 구멍 개수 세기"로 판정** (부품이 정사각 블록 조합이 아니라 다공 막대이기 때문)
- 세로 막대 구멍 개수 + 가로 막대 구멍 개수(모서리 겹침 지점 제외)를 Recipe의 `final_shape_check`와 비교
- **불일치 시 텍스트 설명 자동 생성**: 예상값과 실제 감지값을 직접 비교하는 산술 로직이므로, 모든 실패 케이스를 미리 학습시킬 필요 없음 (예: "가로 방향 구멍이 1개 부족합니다")
#### 딥러닝 분류 (확정)

- 완성된 조립체를 **Model A/B/C 중 하나로 분류**한다. (구현 필수 항목)
 
- 후보: **ResNet-18(1순위, 파인튜닝 쉽고 자료 많음)**, EfficientNet-B0(경량 대안)
- **ViT는 데이터 규모상 비추천** (클래스당 수십 장 규모엔 과함)
- **PatchCore는 사용하지 않음** — 이상탐지(정상/비정상) 모델이라 다중 클래스 분류(A/B/C 판별)에 구조적으로 안 맞음
- **역할 분리**: 딥러닝 분류기는 "어디가 틀렸는지" 텍스트 설명을 생성하지 않음 (블랙박스라 구조적으로 불가능). 룰베이스가 텍스트 설명을 전담하고, 딥러닝 분류는 룰베이스 결과와 비교하는 **이중 검증/방법론 비교용**으로만 사용.
---
 
## 하드웨어 / UI
 
- **하드웨어(Arduino, LED, 부저) 완전히 제외.** 모든 피드백은 웹 UI로만 표시.
- **웹 UI 프레임워크: FastAPI + 순수 HTML/JS (확정).** 영상은 MJPEG 스트리밍, 상태/경고는 JSON(WebSocket 또는 폴링)으로 전달. 파이프라인은 UI와 분리된 루프로 동작.
- 웹 UI 상세 화면 구성은 별도 확정 필요 (아래 "아직 미정" 참고).
---
 
## Recipe 스키마 (예시)
 
```json
{
  "MODEL_A": {
    "steps": [
      {
        "step": 1,
        "expected_class": "나무_5구멍",
        "description": "가로대(5구멍) 조립대 섹션으로 이동"
      },
      {
        "step": 2,
        "expected_class": "나무_3구멍",
        "description": "세로대(3구멍) 조립대 섹션으로 이동"
      },
      {
        "step": 3,
        "expected_class": "볼트_주황",
        "description": "모서리 결합용 주황 볼트 삽입"
      }
    ],
    "final_shape_check": {
      "vertical_holes": 4,
      "horizontal_holes": 3,
      "joint_class": "볼트_주황"
    }
  },
  "MODEL_B": {
    "steps": [
      { "step": 1, "expected_class": "나무_3구멍", "description": "..." },
      { "step": 2, "expected_class": "나무_2구멍", "description": "..." },
      { "step": 3, "expected_class": "볼트_노랑", "description": "..." }
    ],
    "final_shape_check": {
      "vertical_holes": 3,
      "horizontal_holes": 3,
      "joint_class": "볼트_노랑"
    }
  },
  "MODEL_C": {
    "steps": [
      { "step": 1, "expected_class": "나무_2구멍", "description": "..." },
      { "step": 2, "expected_class": "나무_2구멍", "description": "..." },
      { "step": 3, "expected_class": "볼트_노랑", "description": "..." }
    ],
    "final_shape_check": {
      "vertical_holes": 2,
      "horizontal_holes": 2,
      "joint_class": "볼트_노랑"
    }
  }
}
```
 
**⚠️ `vertical_holes`/`horizontal_holes` 값은 임시 예시임 — 실제 부품 실측 후 정확한 값으로 교체 필요.** DB는 사용하지 않고 이 JSON(또는 Python dict) 형태를 그대로 하드코딩해서 사용.
 
---
 
## 저장소 구조 (제안)
 
```
pokayoke-assembly-verification/
├── data/
│   ├── iconic/                 # 정적 촬영 (검은 배경)
│   │   └── {class_name}/
│   ├── picking/                 # 동적 촬영 프레임 (검은 배경, 손 포함)
│   │   └── {class_name}/
│   ├── assembled/                # 완성 조립체 사진 (DL 분류 트랙용)
│   │   └── {model_name}/
│   └── labels/                   # YOLO 포맷 바운딩 박스
├── src/
│   ├── detection/
│   │   └── train_yolo.py
│   ├── rule_based/
│   │   └── hole_count_check.py    # 구멍 개수 기반 형태 판정
│   ├── classification/            # 딥러닝 분류 트랙 (확정)
│   │   └── train_resnet.py
│   ├── recipe/
│   │   └── recipe.json
│   ├── state_machine.py
│   └── pipeline.py
├── web/                            # 웹 UI
├── checkpoints/
├── requirements.txt
└── README.md
```
 
---
 
## 아직 미정 (구현 전 확인 필요)

- **조립 섹션 검증 디바운스 값**: NG/PASS 확정에 필요한 연속 프레임 수 또는 시간 (config, 초기값 미정 — 제안 후 확인)
- **너트_파랑 처리**: Recipe 예시 스텝에 너트가 없음. 조립 섹션에 너트가 놓이면 NG가 되므로, 스텝에 너트를 넣을지 별도 예외 규칙을 둘지 확정 필요
- **y_threshold 값**: 부품/조립 섹션 경계선 좌표 (config, 카메라 고정 후 실측) 
- Model A/B/C의 정확한 구멍 개수 (실측 후 Recipe JSON 값 확정)
- 픽킹 영상 프레임 추출 수치가 실제 웹캠 fps 기준으로 목표 수량과 맞는지 검증 (데이터 수집 담당자 확인)
- VERIFY_FINAL 실패 시 재시도 세부 동작 (전체 재시작인지, 틀린 부분만 다시 하는지)
- 웹 UI 상세 화면 구성 (와이어프레임 없음)
- YOLO 버전 최종 선정 (YOLOv8/YOLO11 등 중 실험 후 결정)